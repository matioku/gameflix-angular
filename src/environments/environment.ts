/**
 * Configuration très simple pour l'URL du backend GameFlix.
 *
 * Pendant le développement, le front tourne souvent sur le port 4200 et le back sur le 3000.
 * Pour la mise en ligne, tu remplaceras cette URL par celle du serveur réel.
 */
export const environment = {
  apiUrl: 'http://localhost:3000/api',
};
